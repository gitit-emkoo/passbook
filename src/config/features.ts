/**
 * Feature flags for enabling/disabling features
 * Used for gradual rollout and testing
 */
export const featureFlags = {
  students: {
    useStub: false,
  },
  contracts: {
    useStub: false,
  },
  settlements: {
    useStub: false,
  },
  dashboard: {
    useStub: false,
  },
};

